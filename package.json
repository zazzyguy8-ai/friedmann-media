{
  "name": "ai-blocker",
  "version": "1.0.0",
  "description": "AI Blocker — give users full control over AI on the web.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-icons.mjs && node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.281",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.3"
  }
}
