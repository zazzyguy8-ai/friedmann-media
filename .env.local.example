# This site is fully static - it needs no secrets to build or run.
#
# The only value that must be correct before launch is the Cal.com booking
# link, and it lives in lib/site.ts rather than the environment so it can be
# reviewed in a pull request rather than silently changed in a hosting panel.
